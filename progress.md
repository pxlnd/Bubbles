Original prompt: давай соберем игровую сцену, будем постепенно добавлять элементы, которые буду тебе объяснять

- Создан минимальный каркас проекта с одной canvas-сценой.
- В план первой итерации входит базовая 3D-сцена: плоскость, освещение и цветной шар.
- Базовая 3D-сцена собрана: зеленая плоскость, розовый шар, свет и легкая анимация покачивания.
- Добавлены `window.render_game_to_text` и `window.advanceTime(ms)` для детерминированной проверки.
- Проверка через локальный сервер и Playwright пройдена, артефакты сохранены в `output/web-game/`.
- Убрана зависимость от внешнего CDN: сцена теперь рисуется на чистом `canvas`, чтобы работать офлайн и при прямом открытии `index.html`.
- После корректировки проекции шар и плоскость стабильно попадают в кадр.
- Добавлена базовая физика: шар падает под гравитацией, встает на плоскость и катится по наклону.
- Управление наклоном вынесено на стрелки, `Space` плавно выравнивает плоскость.
- Проверены два сценария: свободное падение на плоскость и качение при наклоне вправо; оба без ошибок.
- Камера и проекция подстроены под более низкий фронтальный ракурс с большим количеством неба.
- Для ближнего края плейна добавлен stop-zone clamp: шар доезжает до нижней части экрана и останавливается на плейне, не падая вниз.
- Проверка downhill-сценария пройдена: шар сам скатывается к ближнему краю и остается `grounded`.
- Внешний вид сферы обновлен под bubble-стиль: насыщенный красный градиент, крупное белое число `50`, контур, внутренние рефлексы и блики.
- Визуальная локальная ротация сферы теперь завязана на `rollX/rollZ`: номер и surface-markings смещаются по поверхности при качении.
- Плейн стилизован под cartoon-дерево с досками, теплыми градиентами и простыми сучками.
- Подключена текстура [Assets/floor.png](/Users/kotov/Documents/RND_Projects/Bubles/Assets/floor.png) и наложена на плейн с перспективной аппроксимацией; деревянный стиль оставлен как fallback.
- Цифра на сфере больше не анимируется как отдельный overlay: ее позиция и масштаб вычисляются из повернутой локальной нормали сферы.
- Логика видимости цифры обновлена: теперь она затухает и пропадает при уходе на обратную сторону сферы вместо неестественного вертикального сжатия.
- Добавлена желейность сферы: упругий wobble, squash/stretch от ударов и легкая деформация корпуса по направлению движения.
- Сцена перестроена: плейн стал шире и длиннее, дефолтный наклон почти ровный с легким уклоном вперед.
- Камера поднята выше и сильнее наклонена на плейн.
- Добавлены прозрачные стенки по левому, правому и дальнему краю; передний край открыт.
- Убран интерактивный наклон плейна с клавиатуры.
- Текстура пола убрана, плейн снова однотонный.
- Возвращена текстура `floor.png` на плейн в упрощенном режиме: без полосовой аппроксимации, одним `drawImage` в клипнутый полигон.
- Камера дополнительно поднята и сильнее направлена вниз на плейн и шар.
- На экран вынесен debug-пульт камеры с реальными параметрами: `X`, `Y`, `Z`, `Focal`, `Pitch`, `Yaw` (без `Screen Y`).
- Рендер переписан на настоящий Three.js (`vendor/three.min.js`): теперь сцена состоит из реальных 3D-мешей (плейн, сфера, стенки, маркеры).
- Камера теперь настоящая `PerspectiveCamera` с реальными `position` и `rotation` (`pitch/yaw`) и физичным `fov`, а не псевдопроекция 2D.
- Физика сферы обновлена под 3D-плоскость (контакт по нормали, качение по поверхности, ограничения стенками в локальных координатах стола).
- Вернуты настройки позиции/ротации камеры в UI и добавлены отдельные настройки ротации источника света.
- Добавлены тюнинги физики в UI: `Gravity` и `Jelly`.
- Направление скатывания возвращено к исходной ориентации (убран разворот стола по `Y`).
- Освещение плейна усилено (hemi + ambient + directional, double-sided материал, emissive fill), чтобы текстура не выглядела черной.
- После перехода на textured floor headless Playwright-прогоны стали заметно тяжелее; для следующих итераций может понадобиться еще упростить текстурную аппроксимацию или ввести режим low-detail для тестов.
- TODO: на следующем шаге можно добавлять новые игровые объекты и интерактивность по запросу пользователя.
